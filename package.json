{
  "name": "service-reporter",
  "version": "0.0.1-dev",
  "dependencies": {
    "coffee-script": "1.x",
    "q" : "0.x",
    "request" : "2.x",
    "colors" : "0.x"
  },
  "scripts": {
    "test": "mocha --require should --compilers coffee:coffee-script --colors"
  },
  "devDependencies": {
    "mocha": "*",
    "should": "*",
    "sinon": "*"
  }, 
  "engines": {
    "node": "~0.8.x"
  }
}